name: Build Bootloader

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Build with Makefile
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Makefile out-of-tree
        run: make -j
